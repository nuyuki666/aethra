Положи сюда собранный файл лоадера с именем AethraLoader.exe.

Кнопка «Скачать лоадер» в профиле отдаёт этот файл только пользователям
с активной подпиской. Файл не попадает в git (папка в .gitignore).

После замены файла ничего перезапускать не нужно — отдаётся с диска.
